__pycache__/
*.pyc
# H5: was just `.env` (exact match only) — .env.local/.env.production etc.
# were never actually covered, contradicting admin/.env.example's own claim
# that ".env* is gitignored". The negation keeps every .env.example
# tracked regardless of which directory it's in.
.env*
!.env.example
node_modules/
.next/

# Generated audit/apply outputs (uploaded as CI artifacts, not committed)
full-export-audio.csv
full-export-pdfs.csv
audit-report.csv
hidden-log.csv
review-needed.csv
